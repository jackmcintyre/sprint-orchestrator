# Story 1.1: Story with malformed shell fence

## Description

This story file opens a shell fence inside the Verification section but never
closes it, so the adaptor must refuse with a malformed-fence reason naming
this file.

## Verification

```shell
pnpm --dir plugins/sprint-orchestrator test -- story-one
