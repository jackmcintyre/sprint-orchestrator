{
  "name": "crew",
  "version": "0.1.0",
  "private": true,
  "description": "AI Engineering Team v1 plugin — scaffold (see PRD).",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
